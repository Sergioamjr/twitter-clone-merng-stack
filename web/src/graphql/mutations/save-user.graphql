mutation saveUser(
  $userName: String!
  $name: String!
  $password: String!
  $email: String!
) {
  saveUser(
    userName: $userName
    name: $name
    password: $password
    email: $email
  ) {
    name
    token
    email
  }
}
